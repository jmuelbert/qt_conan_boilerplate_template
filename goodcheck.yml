---
rules:
  # id, pattern, message are required attributes.
  - id: com.github.jmuelbert.qt_boilerpalte_template
    pattern: Github
    severity: warning
    message: Do you want to write GitHub?
    glob:
      - '**/*.rb'
      - '**/*.{yaml,yml}'
      - 'public/**/*.html'
    fail:
      - Signup via Github
    pass:
      - Signup via GitHub
